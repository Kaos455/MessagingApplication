// Create A function for all CRUD operations
// SQL Query Builder is a Must due to having to write SQL queries different SQL Queries for different operations

// For example
// SELECT * FROM users
// SELECT * FROM users WHERE email = ? AND password = ?
// SELECT id FROM users WHERE email = ?


const select = async () => {
    // SQL Base Query
    let query = "SELECT";

    // SQL Query Builder
    
}